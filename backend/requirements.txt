# FastAPI 核心
fastapi==0.115.0
uvicorn[standard]==0.30.0
pydantic==2.9.0
pydantic-settings==2.5.0
email-validator==2.2.0
sse-starlette==2.1.2

# 数据库
sqlalchemy[asyncio]==2.0.35
aiosqlite==0.20.0
# asyncpg==0.30.0          # PostgreSQL 驱动（可选，切换回 PostgreSQL 时取消注释）
# psycopg2-binary==2.9.9   # PostgreSQL 同步驱动（可选）
alembic==1.13.0

# Redis
redis[hiredis]==5.2.0

# 认证
pyjwt==2.9.0
python-multipart==0.0.12

# LLM / AI
openai==1.50.0
httpx==0.27.2          # 锁定版本：httpx 0.28+ 移除了 proxies 参数，与 openai 1.50.0 不兼容
httpcore==1.0.9
tiktoken==0.8.0

# 向量数据库
chromadb==0.5.20
# faiss-cpu==1.9.0  # 可选，取消注释以启用 FAISS

# 工具
aiohttp==3.10.0
PyPDF2==3.0.1
python-dotenv==1.0.1

# 搜索（可选）
# duckduckgo-search==6.0.0  # 取消注释以启用联网搜索

# 日志
loguru==0.7.2
